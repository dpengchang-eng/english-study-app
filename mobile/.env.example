# Optional. Gemini Developer API key if Firebase AI Logic is not enabled
# on project english-study-app-c645a. Never commit the real key.
EXPO_PUBLIC_GEMINI_API_KEY=

# Optional. App Check debug token (only if you turn on App Check later).
EXPO_PUBLIC_APPCHECK_DEBUG_TOKEN=
